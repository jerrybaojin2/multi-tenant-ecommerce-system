# packages/backend 本地开发环境变量示例。
# 复制为 .env 并按需修改；.env 已被 .gitignore 忽略。
#
# 启动 PG：在本目录运行 `docker compose up -d`（需 Docker）。

DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cool_dev
