---
description: 控制器(Controller)
globs:
---
# 控制器

本项目使用自研 Midway.js 3.x 后端。Controller 只负责 HTTP 路由、参数读取、权限入口和响应包装，业务流程放到 Service。

## 基本写法

```ts
import { Body, Controller, Get, Inject, Param, Post } from '@midwayjs/core';
import { OrderService } from '../service/order';

@Controller('/admin/order')
export class AdminOrderController {
  @Inject()
  orderService: OrderService;

  @Get('/:id')
  async info(@Param('id') id: string) {
    return this.orderService.info(id);
  }

  @Post('/save')
  async save(@Body() body: SaveOrderDTO) {
    return this.orderService.save(body);
  }
}
```

## 路由规则

- 后台管理接口使用 `/admin/<module>` 前缀。
- 小程序或客户端接口使用 `/app/<module>` 前缀。
- 健康检查、开放回调等非租户接口必须在代码中明确标注用途，并经过生产环境保护审查。
- 不要依赖自动生成 CRUD 路由，所有接口方法都要在 Controller 中显式声明。

## Controller 职责

- 读取 `@Param()`、`@Query()`、`@Body()`、`@Headers()` 等输入。
- 调用 DTO 或 schema 完成参数校验。
- 从中间件或 tenant context 获取当前租户、用户、权限上下文。
- 调用 Service 并返回统一响应。

## 禁止事项

- 不在 Controller 中拼接 SQL。
- 不在 Controller 中直接访问 Repository，除非是极小的只读健康检查。
- 不从请求体信任 `tenant_id`；租户只能来自已验证的 tenant context。
- 不添加旧框架控制器装饰器、旧通用控制器基类或自动 CRUD 配置。
- 不暴露开发元数据接口到生产环境。
