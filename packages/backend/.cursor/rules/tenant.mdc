---
description: 多租户(Tenant)
globs:
---
# 多租户

本项目采用单库多租户模型。应用层使用 tenant context，数据层使用 `tenant_id` 条件与 PostgreSQL RLS 双重隔离。

## 租户来源

- HTTP 请求进入后，由认证中间件解析租户。
- 默认租户头为 `x-tenant-id`，也可以由登录态、平台域名或小程序 appid 映射得到。
- 解析完成后写入 tenant context，后续 Controller 和 Service 只能读取该上下文。
- 请求体、查询字符串中的租户字段不能作为可信来源。

## Entity 字段

```ts
import { Column, Index } from 'typeorm';

export abstract class TenantScopedEntity {
  @Index()
  @Column({ name: 'tenant_id', type: 'uuid' })
  tenantId: string;
}
```

- 租户表必须包含 `tenant_id`。
- 业务唯一约束需要把 `tenant_id` 纳入组合唯一索引。
- 查询索引优先包含 `tenant_id`，再按业务字段排序。

## Service 查询

```ts
const tenantId = this.tenantContext.requireTenantId();
return this.orderRepo.findOneBy({ id, tenantId });
```

- 读写租户表时必须附带当前租户。
- 新增数据时由 Service 或仓储封装写入 `tenantId`。
- 批量更新、删除必须同时限定主键和租户。

## RLS 约定

- 每个租户表开启 Row Level Security。
- Policy 以连接变量 `app.tenant_id` 为准。
- 请求级事务开始时设置该变量，结束后释放连接。
- 后台任务如果处理多个租户，必须逐租户设置上下文并分批提交。

## 平台级操作

- 平台级统计、迁移、审计可以跨租户，但必须走单独服务方法。
- 方法名应清楚表达系统级语义，例如 `platformListAcrossTenants`。
- 记录操作者、原因、影响范围和执行结果。

## 禁止事项

- 不创建绕过租户隔离的临时 helper。
- 不在业务接口中接受外部传入的租户覆盖值。
- 不关闭 RLS 来修复业务查询。
- 不把开发环境租户宽松策略复制到生产配置。
