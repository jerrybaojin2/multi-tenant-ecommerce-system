---
description: 即时通信(Socket)
globs:
---
# 即时通信

即时通信连接必须经过认证，并绑定 tenant context。所有房间、频道和消息都要带租户边界。

## 规则

- 连接握手阶段校验 token，并解析 `tenantId` 与用户身份。
- 房间名包含租户前缀，例如 `tenant:${tenantId}:order:${orderId}`。
- 服务端推送前再次校验当前用户是否可见该业务对象。
- 不向跨租户公共房间广播租户业务数据。
- 断线重连后重新建立订阅，不信任客户端缓存的订阅列表。
