---
description: 数据库(db)
globs:
---
# 数据库

本项目固定使用 PostgreSQL + TypeORM 0.3.x。开发、测试、生产都应保持同一数据库方言，避免 MySQL 或 SQLite 专属写法进入业务代码。

## 配置示例

```ts
import { MidwayConfig } from '@midwayjs/core';

export default {
  typeorm: {
    dataSource: {
      default: {
        type: 'postgres',
        host: process.env.DB_HOST || '127.0.0.1',
        port: Number(process.env.DB_PORT || 5432),
        username: process.env.DB_USER || 'postgres',
        password: process.env.DB_PASSWORD || 'postgres',
        database: process.env.DB_NAME || 'rent_dev',
        synchronize: process.env.NODE_ENV !== 'production',
        logging: process.env.TYPEORM_LOGGING === 'true'
      }
    }
  }
} as MidwayConfig;
```

## Entity 规则

```ts
import { Column, CreateDateColumn, Entity, Index, PrimaryGeneratedColumn, UpdateDateColumn } from 'typeorm';

@Entity({ name: 'rent_order' })
@Index(['tenantId', 'createdAt'])
export class OrderEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @Column({ name: 'tenant_id', type: 'uuid' })
  tenantId: string;

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;

  @UpdateDateColumn({ name: 'updated_at' })
  updatedAt: Date;
}
```

- 表名使用下划线命名。
- 实体属性使用 camelCase，数据库列名用 `name` 显式映射。
- 租户表必须有 `tenant_id`，并建立包含 `tenant_id` 的索引。
- 关系可以用外键字段表达，避免在高频路径滥用懒加载关系。
- 审计字段由项目自研实体约定维护，不引用旧框架实体基类。

## PostgreSQL RLS

- 租户表必须开启 Row Level Security。
- RLS policy 使用当前连接变量，例如 `current_setting('app.tenant_id', true)`。
- 每个请求进入事务或查询前，必须设置当前租户变量。
- 迁移或平台任务需要跨租户时，必须使用受控数据库角色或显式审计流程。

## 生产保护

- 生产环境必须设置 `synchronize: false`。
- schema 变更走迁移脚本。
- 不在生产暴露开发元数据、实体扫描详情或调试接口。
- 不把数据库密码、JWT secret、对象存储密钥写入配置示例。
