---
description: 任务(Task)
globs:
---
# 任务

任务用于定时处理、队列消费和后台批处理。任务代码必须显式处理租户上下文和生产环境保护。

## 定时任务

```ts
import { Provide } from '@midwayjs/core';

@Provide()
export class OrderExpireTask {
  async run() {
    // 查询待处理租户，逐租户设置 tenant context 后执行。
  }
}
```

## 队列任务

- 队列消息只保存必要字段，例如 `tenantId`、业务 id、幂等键。
- 消费前恢复 tenant context，并设置 PostgreSQL RLS 连接变量。
- 任务必须具备幂等性，重复消费不能造成重复扣款、重复发货或重复状态流转。
- 失败重试需要设置上限，并记录最后错误。

## 生产保护

- 会修改大量数据的任务必须检查运行环境和开关。
- 危险任务需要 dry-run 模式或影响范围预估。
- 跨租户任务逐租户处理，保留审计日志。
- 不在任务中关闭租户隔离或 RLS。
