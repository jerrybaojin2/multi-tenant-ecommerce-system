---
description: 模块(Module)
globs:
---
# 模块

模块按业务域组织，目录保持简单、可扫描、可迁移。

## 推荐结构

```text
src/modules/order
  controller
    admin.ts
    app.ts
  dto
    save.ts
  entity
    order.ts
  service
    order.ts
```

## 规则

- 文件名使用小写和下划线；同一模块内避免重复业务前缀。
- Controller 分后台端和客户端入口，Service 尽量按领域聚合。
- Entity 显式声明表名、列名、索引和租户字段。
- DTO 不复用 Entity，避免把数据库字段直接暴露为接口契约。
- 模块间调用优先通过 Service，不直接跨模块操作对方 Repository。
- 新模块如果包含租户数据，必须同时补齐租户字段、索引、RLS policy 和测试计划。
