---
description: 事件(Event)
globs:
---
# 事件

事件用于模块间解耦、异步通知和审计记录。事件载荷必须携带必要的租户与操作者上下文。

## 写法

```ts
import { Inject, Provide } from '@midwayjs/core';
import { EventBus } from '../../../core/event/bus';

@Provide()
export class OrderEventPublisher {
  @Inject()
  eventBus: EventBus;

  async orderCreated(payload: OrderCreatedPayload) {
    await this.eventBus.emit('order.created', payload);
  }
}
```

## 规则

- 事件名使用 `<domain>.<action>`。
- 租户数据事件必须包含 `tenantId`。
- 消费端处理租户数据前必须恢复 tenant context。
- 事件处理失败需要可观测日志，重要事件要具备重试或补偿能力。
- 不在事件载荷中放明文密钥、完整 token 或敏感隐私字段。
