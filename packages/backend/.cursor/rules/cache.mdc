---
description: 缓存(Cache)
globs:
---
# 缓存

缓存只做性能优化，不能作为权限、租户隔离或资金状态的唯一依据。

## Key 规则

```ts
const key = `tenant:${tenantId}:order:${orderId}`;
```

- 租户数据缓存 key 必须包含 `tenantId`。
- 平台级缓存 key 必须使用 `platform:` 前缀并注明用途。
- 缓存值不要包含明文 token、密码、密钥或完整身份证件信息。
- 写操作完成后要主动删除或更新相关缓存。

## 使用建议

- 高频只读字典、租户配置、权限摘要适合缓存。
- 订单状态、支付状态、库存状态必须以数据库为准。
- TTL 根据业务变化频率设置，不要无限期缓存租户业务数据。
