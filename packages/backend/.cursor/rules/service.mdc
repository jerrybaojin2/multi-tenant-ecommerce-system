---
description: 服务(Service)
globs:
---
# 服务

Service 承载业务流程、事务边界、仓储访问与领域规则。Controller 不直接处理复杂业务。

## 基本写法

```ts
import { Inject, Provide } from '@midwayjs/core';
import { InjectEntityModel } from '@midwayjs/typeorm';
import { Repository } from 'typeorm';
import { TenantContext } from '../../../core/tenant/context';
import { OrderEntity } from '../entity/order';

@Provide()
export class OrderService {
  @InjectEntityModel(OrderEntity)
  orderRepo: Repository<OrderEntity>;

  @Inject()
  tenantContext: TenantContext;

  async info(id: string) {
    const tenantId = this.tenantContext.requireTenantId();
    return this.orderRepo.findOneBy({ id, tenantId });
  }
}
```

## TypeORM 访问规则

- 首选 Repository API 与 QueryBuilder。
- 查询租户数据时必须附带 `tenantId` 条件，或通过项目自研的租户仓储封装自动注入。
- 写入租户数据时必须由 tenant context 填充 `tenantId`，不要信任外部入参。
- 事务使用 TypeORM DataSource 或项目封装的事务服务。
- 原生 SQL 仅用于统计、RLS 初始化、复杂报表等必要场景，并必须使用参数绑定。

## 跨租户能力

- 跨租户查询只允许出现在平台级管理、迁移脚本、审计任务等明确场景。
- 方法名、注释和调用方必须表达系统级语义。
- 必须记录操作者、目标租户、原因与时间。

## 禁止事项

- 不继承旧通用 Service 基类。
- 不提供临时绕过租户隔离的 helper。
- 不在 Service 中返回未经权限过滤的敏感字段。
- 不把生产环境保护逻辑散落在业务方法里，统一接入 prod guard。
