---
description: 权限(Authority)
globs:
---
# 权限

权限系统由认证、租户上下文、角色能力和资源范围共同组成。任何后台接口都必须明确认证与授权策略。

## 认证

- 登录后签发短期 access token 和可控刷新凭证。
- token 解析后写入 request context，包括 userId、tenantId、roles、scopes。
- 生产环境必须校验密钥强度、签发方、过期时间和撤销状态。

## 授权

- Controller 入口声明所需权限。
- Service 执行业务前校验资源归属与租户边界。
- 平台管理员能力和租户管理员能力分开建模。
- 跨租户操作必须进入平台级审计。

## 禁止事项

- 不用路径前缀代替真实权限判断。
- 不在前端隐藏按钮后省略后端授权。
- 不把开发测试账号、默认密钥或调试后门写入配置示例。
