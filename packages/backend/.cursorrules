# 项目背景

- 后端框架：自研 Midway.js 3.x 应用，运行在 Koa 适配器之上。
- 数据库：PostgreSQL + TypeORM 0.3.x。
- 多租户：所有租户数据必须显式携带 `tenant_id`，并通过 tenant context、TypeORM 查询条件与 PostgreSQL RLS 共同隔离。
- 语言：TypeScript 优先，配置与运行时代码使用 `@midwayjs/core`。

# 目录约定

- `src/config`：Midway 配置文件。
- `src/core`：自研基础能力，例如响应封装、租户上下文、生产环境保护、RLS 初始化等。
- `src/modules/<module>`：业务模块。
- `controller`：只做路由、鉴权入口、参数解析与响应包装。
- `service`：承载业务流程、事务与仓储访问。
- `entity`：TypeORM 实体，字段名按数据库列名显式声明。
- `dto`：接口参数结构与校验。

# 编码规则

- 始终使用中文回复，代码注释也使用中文。
- 使用 `@midwayjs/core`，不要从旧 decorator 包导入。
- Controller 使用 `@Controller()`、`@Get()`、`@Post()` 等 Midway 原生装饰器。
- Controller 不直接拼 SQL，不承载业务流程；复杂逻辑放入 Service。
- Service 使用 `@Provide()`，通过 `@InjectEntityModel()` 注入 TypeORM Repository。
- Entity 不继承旧框架实体基类；需要的审计字段、软删除字段与 `tenant_id` 由项目自研基类或实体本身提供。
- 所有租户表必须包含 `tenant_id`，数据库列名固定为 `tenant_id`，实体属性可使用 `tenantId` 并显式声明 `name: 'tenant_id'`。
- 所有租户查询必须从 tenant context 读取租户，不允许从请求体任意信任租户字段。
- 需要跨租户管理能力时，必须使用显式的系统级服务方法，并保留审计记录；不要提供局部绕过租户隔离的工具。
- 生产配置必须关闭 TypeORM 自动同步，并关闭开发元数据暴露。
- 新增模块前阅读 `.cursor/rules/controller.mdc`、`.cursor/rules/service.mdc`、`.cursor/rules/db.mdc`、`.cursor/rules/tenant.mdc`。
